Study the udp reverse proxy settings of nginx 1.12.

Add debug log out with func name. *Only tested under ubuntu 16*

./configure --prefix=/usr/local/mynginx --with-pcre-jit --with-http_ssl_module --with-http_realip_module --with-http_stub_status_module --with-stream --with-debug

test app is in quickdemo python-udp

---------- original ----------
Documentation is available at http://nginx.org

